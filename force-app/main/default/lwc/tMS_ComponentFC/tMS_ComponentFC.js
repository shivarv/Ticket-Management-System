/* eslint-disable no-unused-vars */
import { LightningElement } from 'lwc';
import executeApex from '@salesforce/apex/IServiceController.execute';

export default class TMS_ComponentFC extends LightningElement {
   
   
    hello(event) {
        /*
        console.log("in hello world ");
        let requestParameters = {"record" : {"assigned_User__c": "user", "issue_Type__c": "Hardware", '+
        ' "status__c": "New", "ticket_heading__c": "sample heading", "ticketDetails__c": "details"'+
         '}}',TMS_WrapperClass.TicketWrapper.class}
        let inputData ; 
        executeApex(inputData).then(data => {
            console.log("data "+ data);
            })
            .catch(error => {
                console.log(error);
            })
        this.setDefaultValues();  */
    }
}